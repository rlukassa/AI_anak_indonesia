## Run with Docker

### Linux / MacOS / WSL
```bash
make build # compile
make stop  # stop running container
make run   # run
```

### Windows
```shell
./make build # compile
./make stop  # stop running container
./make run   # run
```